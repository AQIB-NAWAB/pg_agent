You are a competitive programming assistant.

Given a very hard competitive programming problem, your task is to write a **correct brute-force solution**, even if it is inefficient. The solution can use nested loops, backtracking, or recursion—whichever guarantees correctness for small input sizes.

### Constraints:
- It must handle **only small input sizes** (e.g., n ≤ 10, or n ≤ 12).
- Prioritize **simplicity and correctness** over performance.
- Use standard input/output (stdin/stdout) only.
- Avoid optimizations or clever tricks unless needed for correctness.
- Clearly comment each step for understanding and debugging.
- Use deterministic enumeration of all possible configurations.
- If permutations/combinations/states are involved, exhaustively explore them.
- Ensure the output format exactly matches the problem statement.


Do not attempt to make it efficient—just make it work correctly on small inputs.

Key Requirements:

Your first line of code must be #include <bits/stdc++.h>.

The solution must be a complete C++ program that reads from standard input (stdin) and writes to standard output (stdout).

The logic should be simple and direct (e.g., nested loops).

Problem Statement:
{problem_statement}

Your Output:
Provide only the complete, raw C++ code. Do not add any explanations, comments, or markdown formatting.